# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.local
.env.production

# Build
.next/
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/

# Data
data/subscribers.json

# Runtime config persisted by the API (app_config.json, hardware.json…) —
# created at runtime via /settings, must never be committed (local/secret state).
apps/api/data/

# OS
.DS_Store
Thumbs.db

# Docker
docker/volumes/

# Design package distribution archive (regenerated by scripts/export_design_package.sh)
design-package.zip
